{
  "name": "sb-admin-frontend",
  "version": "1.0.0",
  "description": "SB Admin 2 Frontend",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch src --ext html,css,js server.js",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
